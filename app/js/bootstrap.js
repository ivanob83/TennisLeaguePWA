/**
 * Bootstrap
 * 
 * Application initialization and setup
 * Firebase is initialized in config/firebase.config.js
 */

// If you need to make HTTP requests outside of Firebase,
// you can use the native fetch API or install axios as needed.
// Firebase SDK handles all database and auth operations.
